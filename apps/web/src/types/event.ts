export interface Event {
  // TODO: Define event interface
  id: string;
  name: string;
  description: string;
  // TODO: Add more fields (date, location, organizer, etc.)
}

export interface EventMetadata {
  // TODO: Define event metadata structure for IPFS
}

export interface Ticket {
  // TODO: Define ticket interface
  id: string;
  eventId: string;
  owner: string;
  // TODO: Add more fields (seat, price, etc.)
}

export interface TicketMetadata {
  // TODO: Define ticket metadata structure for NFT
}
